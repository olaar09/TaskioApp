<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context="taskio.taskio.co.taskio.AddTaskActivity"
    tools:showIn="@layout/activity_add_task">

<RelativeLayout
    android:layout_width="match_parent"
    android:background="@color/whitecolor"
    android:padding="10dp"
    android:layout_marginTop="10dp"
    android:layout_height="match_parent"><!-- change this to wrap content when u start milestone so there can be margon abetween both layouts-->
    <EditText
        android:layout_marginTop="10dp"
        android:id="@+id/task_title_input"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Task name"
        android:inputType="text" />

    <EditText
        android:id="@+id/task_title_descr"
        android:layout_marginTop="30dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/task_title_input"
        android:gravity="top|left"
        android:hint="Describe the task briefly"
        android:inputType="textMultiLine"
        android:lines="10"
        android:minLines="6"
        android:scrollbars="vertical" />

</RelativeLayout>

</RelativeLayout>
